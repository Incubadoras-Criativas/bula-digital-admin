<div class="form" id="form_welcome_message">
  <%= form_with model: @appInfo, url: update_welcome_message_path(@appInfo.id) do |f| %>
    <fieldset class="trix-container trix-content">
      <%= f.label :welcome_message, 'Mensagem de boas vindas:' %>
      <%= f.rich_text_area :welcome_message, data: { controller: '_trix', trix_target: 'editor', trix_mode_value: 'text'} %>
      &nbsp;
    </fieldset>

    <fieldset class="btn-form">
      <%= f.submit value: 'Gravar', data: { disable_with: 'Gravando...' } %>
    </fieldset>

    <% msg ||= '' %>
    <% if msg.present? %>
      <%= content_tag :div, msg, class: 'frm-message' %>
    <% end %>

    <% @appInfo.errors.messages.each do |error| %>
      <%= content_tag :div, "#{error[0]} -> #{error[1]}", class: 'msg' %>
    <% end %>
  <% end %>
</div>
